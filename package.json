{
  "name": "my-todo",
  "private": true,
  "scripts": {
    "api:start": "bun run apps/api/index.ts",
    "api:dev": "bun run --watch apps/api/index.ts"
  },
  "workspaces": [
    "apps/*",
    "libs/**/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  }
}